import React, { Fragment } from 'react';
import Loader from '../components/Loader'

const Mail = () => {
    return(
        <Fragment>
            <Loader />
        </Fragment>
    )
}

export default Mail;